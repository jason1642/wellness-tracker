import * as React from 'react';

interface IRecentEntriesProps {
}

const RecentEntries: React.FunctionComponent<IRecentEntriesProps> = (props) => {
  return (
    <div>
        RecentEntries
    </div>
  );
};

export default RecentEntries;
