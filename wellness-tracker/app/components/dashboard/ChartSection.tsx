import * as React from 'react';

interface IChartSectionProps {
}

const ChartSection: React.FunctionComponent<IChartSectionProps> = (props) => {
  return (
    <div>
        ChartSection
    </div>
  );
};

export default ChartSection;
